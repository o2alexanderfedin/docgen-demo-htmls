# Test Mermaid Diagram

This is a simple test file with a Mermaid diagram:

```mermaid
graph TD
    A[Start] --> B[Process]
    B --> C[End]
    B --> D[Alternative End]
```

Let's see if it converts properly.